# Environment Variables Configuration
#
# NOTE: This project uses Cloudflare adapter with two environment files:
# 1. .env - Used by Vite/Astro build system (import.meta.env)
# 2. .dev.vars - Used by Cloudflare platformProxy for local runtime (locals.runtime.env)
#
# For local development, copy this file to both .env and .dev.vars
# The code will automatically use process.env as fallback in Node.js dev mode

SUPABASE_URL=###
SUPABASE_KEY=###
# Public variables for client-side access (Astro requirement)
PUBLIC_SUPABASE_URL=###
PUBLIC_SUPABASE_ANON_KEY=###
PUBLIC_SITE_URL=http://localhost:4321
SUPABASE_ACCESS_TOKEN=###
OPENROUTER_API_KEY=###