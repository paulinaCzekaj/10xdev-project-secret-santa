---
import { Gift, Users, Eye } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
---

<section class="py-16 md:py-24 bg-white">
  <div class="mx-auto max-w-7xl px-6">
    {/* Section Header */}
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        Sama Zabawa, Zero Kłopotu
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Nasza aplikacja ułatwia organizację wymiany prezentów z przyjaciómi, rodziną lub kolegami z pracy.
      </p>
    </div>

    {/* Features Grid */}
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {/* Feature 1 */}
      <Card class="border-2 hover:shadow-lg transition-all hover:-translate-y-1 duration-300 gap-0 py-0">
        <CardContent class="p-8 flex flex-col items-center justify-center text-center w-full">
          <div class="flex items-center justify-center w-16 h-16 rounded-full bg-pink-100 mb-4">
            <Gift className="h-8 w-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            Twórz Loterie
          </h3>
          <p class="text-gray-600">
            Łatwo stwórz nową loterię prezentową za pomocą kilku kliknięć.
          </p>
        </CardContent>
      </Card>

      {/* Feature 2 */}
      <Card class="border-2 hover:shadow-lg transition-all hover:-translate-y-1 duration-300 gap-0 py-0">
        <CardContent class="p-8 flex flex-col items-center justify-center text-center w-full">
          <div class="flex items-center justify-center w-16 h-16 rounded-full bg-pink-100 mb-4">
            <Users className="h-8 w-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            Zarządzaj Uczestnikami
          </h3>
          <p class="text-gray-600">
            Zapraszaj i zarządzaj wszystkimi uczestnikami Twojej wymiany.
          </p>
        </CardContent>
      </Card>

      {/* Feature 3 */}
      <Card class="border-2 hover:shadow-lg transition-all hover:-translate-y-1 duration-300 gap-0 py-0">
        <CardContent class="p-8 flex flex-col items-center justify-center text-center w-full">
          <div class="flex items-center justify-center w-16 h-16 rounded-full bg-pink-100 mb-4">
            <Eye className="h-8 w-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            Zobacz Swojego Odbiorcę
          </h3>
          <p class="text-gray-600">
            Zobacz, dla kogo kupujesz prezent, nie psując niespodzianki innym.
          </p>
        </CardContent>
      </Card>
    </div>
  </div>
</section>
